# Realtime Chat Frontend

Angular application for the Realtime Chat project.

## Scripts

```bash
npm install
npm start
```

The application runs at `http://localhost:4200` and proxies API requests to the Spring Boot backend.

```bash
npm test
npm run build
```
